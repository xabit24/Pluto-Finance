export function TokenPill({ symbol, locked }: { symbol: "SUI" | "USDC"; locked?: boolean }) { return (<div className="flex items-center gap-2 rounded-xl border border-white/15 bg-white/10 px-3 py-2 text-sm"><div className="grid h-6 w-6 place-items-center rounded-full bg-white/90 text-[#16083A] text-xs font-bold">{symbol === "SUI" ? "S" : "$"}</div><span className="font-semibold">{symbol}</span>{locked && <span className="ml-1 text-[10px] uppercase tracking-wider text-white/50">locked</span>}</div>); }